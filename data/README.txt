قاعدة البيانات التشغيلية لا تُرفع إلى GitHub.
عند أول تشغيل ينشئ النظام قاعدة SQLite جديدة تلقائياً.
إذا كانت لديك بيانات قديمة، استخدم UPGRADE-KEEP-OLD-DATA.bat أو استعدها من النسخة الاحتياطية الخاصة بك.
